# Codex Context Handoff

## 2026-08-15：Solutions 六个系列标题完整显示

- 用户反馈 `/solutions.php` 的六张应用系列卡片标题只显示两行并出现省略号，希望字号略缩小且全部显示完整。
- 根因：`assets/css/solutions.css` 将标题固定为 54px、强制两行 `-webkit-line-clamp:2` 并隐藏溢出；线上 HTML 回读确认六个标题原文完整，不是后台数据缺失。
- 标题字号调整为 20–22px，取消行数截断和隐藏，标题行改为最小 78px 的自适应高度，六张卡片继续对齐；规则只作用于 Solutions 顶部六张卡片，不影响说明文字和其他产品卡片。
- `solutions.php` 将样式缓存版本更新至 `solutions.css?v=1.0.45`；新增 `tests/solutions_strip_titles_contract.php`。候选文件已在香港服务器 `/tmp/artdon_solution_titles_20260815` 通过 PHP 语法和契约测试；功能提交 `8c14c76` 已推送 GitHub，部署前备份为 `/tmp/solutions.php.before_8c14c76_20260815`、`/tmp/solutions.css.before_8c14c76_20260815`，随后精确部署正式服务器并清理 7 个页面缓存文件。
- 正式服务器复检通过；线上 `/solutions.php` 已加载 `solutions.css?v=1.0.45`，六个标题 HTML 均为完整原文，线上 CSS 回读确认标题为 20–22px、`overflow:visible` 且不再含两行截断。最终版本以本节上下文提交后的 HEAD 为准。

## 2026-08-13：官网已回复同步广州派工完成

- 官网询盘状态原来只写香港本地，广州官网询盘任务、CRM 待办和派工待办不会随“已回复”完成。
- `includes/sync.php` 新增耐久队列事件 `inquiry.status_changed`；只有 `replied / closed` 且已有广州关联的询盘会同步，单条操作立即尝试发送，失败保留在队列重试；批量状态操作只入队，避免大量跨服务器请求卡住页面。
- `admin/inquiries.php` 的单条与批量状态操作、`admin/contact_page.php` 的“标记已处理/归档”都接入同一状态同步方法；普通 `new / assigned` 状态不改变广州任务。
- 新增 `tests/inquiry_status_sync_contract.php`。候选文件已在香港服务器 `/tmp/artdon_status_candidate_20260813` 通过 PHP 语法与契约测试；功能提交 `108a0ab` 已推送 GitHub，随后备份并精确部署到正式服务器，服务器复检通过。
- 现有询盘 `#69` 已通过新事件补同步，香港队列 `#134` 为 `synced`、广州返回 `status-staging-51`；变更前询盘快照为 `/tmp/hk_inquiry_69_before_status_sync_20260813.json`。广州关联官网任务、CRM 待办和派工待办均已完成。最终版本以本节上下文提交后的 HEAD 为准。

Last updated: 2026-08-15

## Workflow Rule

Default workflow:

```text
Local edit -> Git commit -> GitHub push -> Server deploy
```

Do not edit production server files directly unless it is urgent or explicitly requested.

Excluded from GitHub:

- `uploads/`
- `storage/`
- `website_config.php`
- `.user.ini`
- SQL dumps
- ZIP / archive files
- backups
- temporary repair scripts
- local Codex helper files

## Current Repository

Local project:

```text
/Users/qiulei/Documents/Codex/2026-07-27/tjf/work/artdon_hongkong
```

GitHub remote:

```text
git@github.com:qiulei6386-stack/artdon_hongkong.git
```

Server:

```text
artdon-hongkong:/www/wwwroot/43.132.210.162/
```

## Latest Completed Work

- Unified sitewide contact information across saved backend content, public defaults, footer defaults and Contact page structured data. Canonical values are: `Artdon Lighting Limited`, contact `Sukie`, email `sales@artdon.cn`, telephone `+86-760-22211886`, mobile display `+86-13925332972 / +86-13751710245`, primary WhatsApp `8613925332972`, address `No. 15 Zhihe 3rd Street, Yumin Dongsheng, Xiaolan Town, Zhongshan City, Guangdong, China (Post Code: 528414)`.
- Corrected old inconsistent values: `+86-760-22211896`, `Zhibie` and `Yunin` no longer appear in the relevant source defaults, saved `site/footer/contact` content or representative live public pages. Contact page JSON-LD now uses the canonical telephone, two clean sales contact points and the corrected postal address.
- Created recoverable production backup `/root/artdon_contact_unify_backup_20260801_125610.json` before editing saved contact blocks. Public caches were moved to `storage/page_cache_backup_contact_unify_20260801_125711` and `storage/page_cache_backup_contact_schema_20260801_125817`.
- Verified live homepage, Contact, Products, About and Solutions pages: old bad contact values count 0; canonical email, telephone, WhatsApp and corrected address appear as expected.
- Cleaned public NO IMAGE, duplicate-product and empty-content issues without hard-deleting product records. The cleanup was applied directly to the Hong Kong production database after a full recoverable product/variant backup.
- Unpublished two empty duplicate LUMI series pages (`lumi-surface-mounted-light`, `lumi-surface-mounted-light-2`) because both were published with no variants and no text body. Unpublished the duplicate `LOW VOLTAGE TRACK (Recessed)` variant ending in `-2`. Also aligned variants under unpublished parent series by marking those variants unpublished.
- Filled two published MAGENTRA variants that had empty model images from their parent series cover image, and backfilled 152 published variant intro/short-description fields from their already generated SEO descriptions so published product-detail pages are not text-empty.
- Disabled the stale homepage product slot `id=57` that pointed to unpublished `profocus-outdoor-light-2` and caused `NO IMAGE` to remain in the homepage HTML.
- Created recoverable backups before changes: `/root/artdon_content_cleanup_backup_20260801_124828.json` for product/variant data and `/root/artdon_home_slots_cleanup_backup_20260801_125038.json` for homepage product slots. Cleared six old public HTML cache files into `storage/page_cache_backup_content_cleanup_20260801_125050`.
- Verified after cleanup: published series 45, published variants 153, 0 published series without image, 0 published variants without image, 0 empty published series bodies, 0 empty published variant bodies, 0 duplicate published series-name groups, 0 duplicate published variant model/name groups and 0 active homepage slots with missing/unpublished/no-image targets. Homepage and products page source both contain 0 `NO IMAGE` strings. Sitemap now has 378 URLs and does not contain the cleaned old slugs.
- Backfilled and de-duplicated SEO Title and Meta Description data across the public content tables that already support SEO fields. The main repair was the product center: 46 published series and 167 published variants now have non-empty SEO titles and descriptions.
- Preserved existing manually written SEO values and only filled blanks, except for generated duplicate product titles that were corrected to unique titles. The two old duplicate low-voltage recessed accessory records were given temporary distinct SEO titles; the duplicate/empty-content cleanup item should decide whether to merge, rewrite or remove them later.
- Verified public SEO data after the update: `web_products`, `web_product_variants`, `web_solution_pages`, `web_solution_retail_applications`, `web_resource_pages`, `web_resource_blog_articles`, `web_about_pages` and `web_resource_videos` all report 0 empty SEO titles, 0 empty SEO descriptions and 0 duplicate title groups for published/active records.
- Created a recoverable SEO metadata backup before updating database fields: `/root/artdon_seo_meta_backup_20260801_123856.json`. A cache sweep for this item found no existing HTML cache files to move.
- Live sample checks confirm the new meta output on the ARMI series page, ARMI 45 product page, products page and Hospitality Solutions page.
- Unified canonical URL handling for the public site. The formal public host is `https://artdonlighting.com`; `http://artdonlighting.com`, `http://www.artdonlighting.com` and `https://www.artdonlighting.com` now 301 to the HTTPS apex domain while preserving paths and query strings.
- Added `www.artdonlighting.com` to the live TLS certificate. The deployed Let's Encrypt certificate covers `artdonlighting.com` and `www.artdonlighting.com`, verifies successfully and is valid from 2026-08-01 to 2026-10-30.
- Converted legacy public duplicates to permanent redirects: `/index.php` 301s to `/`, `/Home/Products/` 301s to `/products.php`, `series.php?slug=...` 301s to the pretty series URL and `product.php?slug=...` 301s to the pretty product URL.
- Kept `products.php` as the canonical all-products page because existing navigation, filters and backend-generated links already use it. The products canonical tag and sitemap entry now point to `https://artdonlighting.com/products.php`; the sitemap contains 0 `/Home/Products` entries after the fix.
- Created recoverable production backups before URL/SSL changes: `/root/artdon_url_ssl_backup_20260801_113621`, source backups tagged `url_canonical_20260801_115128`, and `/root/artdon_url_products_backup_20260801_123151`. Production PHP lint passed for every changed PHP file.
- Verified the live redirect matrix: HTTP apex 301, HTTP www 301, HTTPS www 301, old `/index.php` 301, old `/Home/Products/` 301, legacy series query 301 and legacy product query 301. Followed final URLs return HTTP 200 with no redirect loop.
- Rebuilt the SEO sitemap structure. `https://artdonlighting.com/sitemap.xml` is now the stable public sitemap-index endpoint and points to the automatically generated `https://artdonlighting.com/sitemap.php` URL set.
- Corrected `robots.txt` to advertise the formal HTTPS domain sitemap rather than the obsolete server IP address.
- Expanded the dynamic sitemap from 8 static IP-based URLs to 380 current public URLs: core pages, current product categories, canonical pretty product/series URLs, published blog articles, active project details and active Solution Application detail pages.
- Excluded all admin/internal paths, duplicate entries, legacy `series.php?slug` / `product.php?slug` product detail URLs and two known 404 legacy 3-circuit variant routes. Static pages no longer publish a false “today” last-modified date on every sitemap request.
- Created recoverable production backups tagged `seo_sitemap_20260801_104500` before deployment. Production PHP lint passed; XML parsing verified 380 URLs, 0 old-IP URLs, 0 legacy product-detail URLs and 0 duplicate URLs. A complete public HTTP check returned 380/380 final 2xx responses.
- Backfilled existing public Solution Application cards into the new category-aware backend instead of leaving the five new categories empty. The verified record counts are Retail 6, Hospitality 6, Museum & Gallery 6, Office 5, Residential 6 and Outdoor & Landscape 6. Solution detail pages now render these managed cards, preserving their prior titles and images while routing clicks to their editable detail pages.
- Deployed `includes/retail_application_data.php` and `includes/solution_page_template.php` after recoverable production backups tagged `solution_application_backfill_20260730_140249` and moving old page-cache files to `storage/page_cache_backup_solution_application_backfill_20260730_140249`.
- Expanded the former Retail-only Applications backend into `Solution Applications`. It now provides six selectable parent categories: Retail, Hospitality, Museum & Gallery, Office, Residential and Outdoor & Landscape. Each parent category has its own create/edit/delete application list; the five new categories start empty and publish nothing until an application is deliberately created.
- Added category-aware application storage, save/delete routing and a generic public route `solutions-application.php?solution=...&slug=...` for all non-Retail application detail pages. Existing Retail application URLs and content remain unchanged.
- Deployed `admin/_layout.php`, `admin/retail_applications.php`, `admin/retail_applications_action.php`, `admin/save_retail_application.php`, `includes/retail_application_data.php`, `includes/retail_application_template.php` and `solutions-application.php` after recoverable production backups tagged `solution_application_categories_20260730_121334` and moving old page-cache files to `storage/page_cache_backup_solution_application_categories_20260730_121334`.
- Added create and delete controls to `admin/retail_applications.php`. The left application list now has a form for creating a new Retail Application (name plus optional slug) and every card has a confirmed delete action; one application must remain so the application set can never become empty.
- New applications receive a full editable default template, appear in the application list immediately, clear public cache on creation/deletion and open through the new dynamic public route `solutions-retail-application.php?slug=...`. The six original application URLs remain unchanged.
- Deployed `admin/retail_applications.php`, `admin/retail_applications_action.php`, `includes/retail_application_data.php` and `solutions-retail-application.php` after recoverable production backups tagged `retail_application_manage_20260730_115134` and moving old page-cache files to `storage/page_cache_backup_retail_application_manage_20260730_115134`.
- Made every editable element of the public Projects top banner available in the admin: image, image ALT, large title and small descriptive copy. The controls are in `Projects 详情页 → 项目页顶部横幅`; saving them clears the public cache and does not affect project-card images.
- Deployed `project.php`, `admin/project_action.php` and `admin/project_details.php` after recoverable production backups tagged `project_banner_copy_20260729_145645` and moving the prior page-cache files to `storage/page_cache_backup_project_banner_copy_20260729_145645`.
- Made the first large image on `project.php` independently editable in the admin: `Projects 详情页 → 项目页顶部横幅` now supports direct upload, media-library selection, image ALT text and automatic public-cache clearing.
- The public Projects page prioritizes this saved banner image while retaining the former `featured-retail.webp` image as a safe fallback; project-card images are unaffected.
- Deployed `project.php`, `admin/project_action.php` and `admin/project_details.php` after creating recoverable production backups tagged `project_banner_admin_20260729_144433` and moving older page-cache files to `storage/page_cache_backup_project_banner_admin_20260729_144433`.
- Updated the shared series-page Projects image treatment from 4:3 to 16:9, reducing image-box height by 25% through center cropping (`object-fit: cover`) without distortion.
- Verified the live EMMA series page: all three project images changed from 597×448 to 597×336, with no horizontal overflow or browser errors.
- Deployed `series.php` after creating the recoverable backup `series.php.bak_project_crop_20260729_110519` and moving previous page-cache files to `storage/page_cache_backup_project_crop_20260729_110519`.
- Expanded product-variant accessory capacity from 8 to 12 across variant editing, normalization and shared accessory push logic.
- Kept series and sibling product cards compact by showing the first four accessories plus a `+N more accessories` indicator when more exist.
- Updated product-detail compatible-accessory layouts for up to 12 entries: four columns on desktop, two on tablet and one on mobile, with incomplete final rows centered.
- Verified the live `1 CIRCUIT TRACK AND ACCESSORIES` series: both products contain eight accessories, each card shows four plus `+4 more accessories`, and each product detail shows all eight in two desktop rows without overflow or console errors.
- Deployed eight accessory-related files after creating recoverable production backups tagged `accessory12_20260728_164912`; moved the old page cache into `storage/page_cache_backup_accessory12_20260728_164912`.
- Simplified the shared series-page Project Support CTA across all product families: removed `Request Sample` and `Download Datasheet`, retained one `Get a quote` button, and right-aligned it at desktop and responsive widths.
- Corrected both product-detail layouts so `Technical files` points to the current product pretty URL plus `#technical-files`, rather than resolving through `<base href="/">` to the homepage.
- Added deterministic same-page scrolling to the `Downloads / Planning files` section with a 96px top offset and cache-busted the product CSS/JS references.
- Verified the live ARMI series page renders exactly one Project Support action with no legacy sample/datasheet buttons; verified the ARMI 73 product page exposes the correct same-product anchor, target heading and updated scroll handler.
- Deployed `series.php`, `product.php`, `assets/js/artdon_product_inline_v718.js` and `assets/css/artdon_product_inline_v718.css`; production PHP lint and JS syntax checks passed.
- Created recoverable production backups with tags `20260727_191801`, `20260727_192150` and `20260727_192547`, and moved regenerated public-page caches into matching backup directories.
- Corrected the footer contact CTA from `index.php#about` to `/about.php`, cleared the remaining public-page caches, and verified both `/products.php` and the homepage render the same 9/7/7/5 footer columns.
- Click-tested the footer `About Us` link from `/products.php`; it opens `/about.php` successfully.
- Backed up `includes/footer.php` and footer content with tag `20260727_153408`, then moved five remaining page-cache files to `storage/page_cache_backup_footer_about_20260727_153419`.
- Rebuilt the public footer navigation as exactly four groups: Products, Solutions, Projects and Resources.
- Added all current product categories, corrected Solutions links to their dedicated pages, completed Projects categories, and replaced legacy Resources destinations with the current Resources pages.
- Persisted footer navigation version `718173`; the live column link counts are Products 9, Solutions 7, Projects 7 and Resources 5.
- Verified all 28 footer navigation links return HTTP 200, the live footer has no horizontal overflow, and the browser console has no errors.
- Backed up production footer code with tag `20260727_152439`, saved the previous footer content as `storage/footer_before_nav_20260727_152439.json`, and moved 16 old page-cache files to `storage/page_cache_backup_footer_nav_20260727_152524`.
- Unified every public `GET A QUOTE` entry through the shared `Project inquiry` modal, including header, homepage hero, series, product-detail and content-page links.
- Added public modal-copy controls under `admin/settings.php` in the `全站询价弹窗` section.
- Added separate task-title, project and dispatch-content templates under `admin/inquiry_routing.php`.
- Changed shared quote submissions to use `source=global_quote` and `support_type=quotation`.
- Verified live behavior on the homepage, ARMI series page, ARMI 45 model page and About page: current URL is retained, the shared modal opens, the legacy homepage modal stays closed, product context is prefilled, and the browser console has no errors.
- Deployed five inquiry-related files after creating production backups with tag `20260727_151137`.
- Moved 31 pre-deployment page-cache files to the recoverable directory `storage/page_cache_backup_quote_unify_20260727_151210`.
- Updated the series-page hero `Get a Quote` action to open the shared CRM inquiry modal used by the floating inquiry button.
- Verified the ARMI series page keeps the current URL, opens `Project inquiry`, shows `Inquiry about: ARMI`, and pre-fills `I am interested in ARMI`.
- Deployed only `includes/floating_actions.php` after creating a production backup.
- Cleared and regenerated the ARMI series-page micro-cache after deployment.
- Initialized local Git repository.
- Connected GitHub with SSH.
- Synced server code to local.
- Excluded uploads, storage, runtime config and backup files from GitHub.
- Pushed initial server code to GitHub.
- Added workflow rules.
- Added context handoff rule and this context file.
- Confirmed standing rule: before exit, update context, commit, push GitHub, and sync server.

## Latest Known Commits

- `0414ef6` - Unify canonical URLs and www SSL
- `b816dd1` - Fix sitemap and robots SEO discovery
- `d942bfe` - Manage blog categories in admin
- `b4a4ec3` - Add editable browser tab icon
- `ec00724` - Backfill solution application management
- `0ce648a` - Document solution application category management
- `ca709ed` - Manage applications for all solution categories
- `10ae7ca` - Document retail application management controls
- `0c77153` - Add retail application create and delete controls
- `46bb946` - Document editable projects banner copy
- `7a294b5` - Make projects banner copy editable
- `c1b0cc3` - Document editable projects banner
- `bb5a939` - Make projects page banner editable
- `7fd6edb` - Crop series project images to widescreen
- `6ab03a0` - Expand product accessory display capacity
- `8c5c960` - Make files anchor scrolling deterministic
- `d3bd0e8` - Ensure product files link scrolls to downloads
- `2023166` - Simplify support CTA and fix files anchor
- `00e23e7` - Point footer About Us to about page
- `2809ff5` - Align footer navigation with public sections
- `4ffb825` - Unify quote modal and configure inquiry dispatch fields
- `89310a2` - Open series quote CTA in inquiry modal
- `aa79f0a` - Initial sync from Hong Kong website server
- `45f65a1` - Document local GitHub server workflow
- `8dbc2d9` - Add Codex context handoff rule

## Server Sync Status

- `includes/schema.php`, `index.php`, `products.php`, `series.php`, `product.php`, `project.php`, `includes/project_detail_template.php`, `includes/solution_page_template.php` and `includes/retail_application_template.php` are synced to the Hong Kong server with unified structured data output. Production PHP lint passed for all nine files; public validation confirmed parseable JSON-LD on 8 representative pages with Organization, WebSite, WebPage/CollectionPage/ItemPage, BreadcrumbList, ProductGroup/Product, Article, Service and ItemList nodes. Server source backup: `/root/artdon_schema_backup_20260801_132544`; page cache backup: `storage/page_cache_backup_schema_20260801_132621`.
- Google Search Console property `sc-domain:artdonlighting.com` was verified using DNS TXT `google-site-verification=EhoBjHYow-Cui7O1YpMGeWLjCmay1eZG5ihEesX9eug` added at the Tencent/DNSPod root host. GSC sitemaps were submitted and verified: `https://artdonlighting.com/sitemap.php` shows status `成功` with 378 discovered pages, and `https://artdonlighting.com/sitemap.xml` shows status `成功` as the sitemap index.
- `includes/seo_internal_links.php`, `products.php`, `series.php`, `product.php`, `project.php`, `includes/project_detail_template.php`, `includes/solution_page_template.php` and `includes/retail_application_template.php` are synced to the Hong Kong server with a visible keyword-focused internal-link module on product, product family, product detail, project, project detail, solution and application-detail pages. Link targets were checked with 0 bad links; production PHP lint passed for all eight touched files. Front-end samples confirmed the module appears on 7 representative pages with 10 related links each. Server backup: `/root/artdon_internal_links_backup_20260801_135049`; cache backup: `storage/page_cache_backup_internal_links_20260801_135111`.
- Mobile speed / Core Web Vitals pass is complete for low-risk infrastructure and LCP improvements. Nginx static asset caching was updated so images/video/fonts (`webp`, `avif`, `ico`, `svg`, `mp4`, `webm`, `woff`, etc.) cache for 180 days with immutable cache-control, and CSS/JS cache for 30 days. Gzip was verified for CSS and HTML. Product family, product detail, project, project detail, solution and application-detail templates now preload their main hero/product image and mark it high priority/eager. Production PHP lint passed for all six touched templates. Nginx backup: `/root/artdon_nginx_speed_backup_20260801_135551.conf`; template backup: `/root/artdon_speed_templates_backup_20260801_135632`; cache backup: `storage/page_cache_backup_speed_20260801_135647`.
- Deeper product-list speed follow-up is synced to the Hong Kong server. `products.php` now preloads the first visible catalogue card image and marks only that first card image eager/high priority while keeping the remaining catalogue images lazy. Production PHP lint passed and the live `/products.php` page verifies with one image preload, one eager image and retained lazy loading. Server backup: `products.php.bak_speed_products_20260801_140337`; cache backup: `storage/page_cache_backup_products_speed_20260801_140407`.
- Mobile homepage video follow-up is synced to the Hong Kong server. `assets/js/artdon_home.js` now skips loading the hero MP4 on mobile screens, browser data-saver mode and slow 2G/3G connections, leaving the poster image as the mobile hero instead. All public references to `artdon_home.js` were cache-busted to `v=6.12.13`. Production PHP lint passed for the touched PHP files; live checks confirm the homepage references `v=6.12.13`, the JS contains the mobile/data-saver/slow-network guard, and the live hero video remains `preload="none"` / `data-src` deferred. Server backup: `/root/artdon_mobile_video_skip_backup_20260801_140813`; cache backup: `storage/page_cache_backup_mobile_video_skip_20260801_140813`.
- Logo/favicon asset optimization is synced to the Hong Kong server. `assets/img/logo-artdon.png` was reduced from about 100KB to 36KB, `assets/img/logo-artdon-footer.png` from 36KB to 24KB and `assets/img/favicon-artdon.png` from 64KB to 20KB. Local static logo/favicon URLs now get a filemtime version query via `web_asset_versioned_url()` so long-cache browsers receive refreshed assets after replacement, while external/custom uploaded URLs remain untouched. Production PHP lint passed for `includes/bootstrap.php`, `partials/header.php` and `partials/footer.php`; live checks confirm favicon/header/footer logo URLs return HTTP 200 with versioned URLs. Server backup: `/root/artdon_logo_favicon_opt_backup_20260801_141230`; cache backup: `storage/page_cache_backup_logo_favicon_opt_20260801_141230`.
- Post-optimization live speed/resource audit is complete with no code deployment required. Representative public pages (`/`, `/products.php`, ARMI series, ARMI variant, `/solutions-hospitality.php`, `/project.php`) all return HTTP 200, keep one eager/high-priority first image where applicable, retain lazy loading for remaining images, and the homepage hero video remains deferred via `preload="none"` plus `data-src`. Response-header checks confirm HTML gzip with short public cache, CSS/JS gzip with 30-day immutable cache and image/favicon assets with 180-day immutable cache.
- SEO content quality audit started. The sitemap currently exposes 378 public URLs. Database-level review found many apparent issues in non-public operational tables (`web_audit_logs`, `web_media`, `web_visit_pageviews`, inquiry logs), which should not be treated as public SEO problems. The first public-data cleanup updated the six `web_solution_pages` compatibility/list records with more specific SEO titles/descriptions and cleared page cache; original values were backed up to `/root/artdon_solution_seo_backup_20260801_143900.json`. Follow-up live checks showed the actual solution detail pages are mainly rendered from the newer Solutions detail content source and already have richer front-end meta descriptions, so future solution SEO edits should be made through `后台 → Solutions 详情页` rather than only `web_solution_pages`. Cache backup: `storage/page_cache_backup_solution_seo_20260801_143900`.
- Product category SEO meta fix is synced to the Hong Kong server. `products.php` now hydrates final front-end category rows with SEO title/description data from `web_product_categories`, including legacy slug aliases for `surface-pendant-lights`, `led-strips-profiles` and `track-systems-accessories`. Production PHP lint passed. Live verification confirmed all 9 sitemap product-category URLs have distinct category-specific meta descriptions with no duplicate description groups. Server backup: `products.php.bak_category_seo_20260801_145200`; cache backups: `storage/page_cache_backup_product_category_seo_20260801_145200`, `storage/page_cache_backup_product_category_seo_alias_20260801_145200`.
- Product series/detail SEO audit is complete. `web_products` (45 published series) and `web_product_variants` (153 published variants) were checked for missing, short, long and duplicate SEO titles/descriptions. Only one issue was found: the SPECTRUM series meta description was 204 characters. It was shortened to 156 characters while preserving accessory, beam-control and retail/commercial relevance. Follow-up audit now reports 0 issues, 0 duplicate title groups and 0 duplicate description groups across both tables. Original SPECTRUM row backup: `/root/artdon_spectrum_series_seo_backup_20260801_150900.json`; cache backup: `storage/page_cache_backup_spectrum_seo_20260801_150900`.
- Resources/blog/video SEO audit is complete. `web_resource_blog_articles` (78 published articles), `web_resource_pages` (6 resource landing pages) and `web_resource_videos` (19 active videos) were checked for missing/short/long/duplicate SEO titles and descriptions. Fixes shortened 5 resource page meta records, 1 blog article description and 18 video descriptions while preserving topic relevance. Follow-up audit now reports 0 issues, 0 duplicate title groups and 0 duplicate description groups across all three resource tables. Original values backup: `/root/artdon_resources_seo_backup_20260801_152300.json`; cache backup: `storage/page_cache_backup_resources_seo_20260801_152300`.
- Project listing/detail SEO template fix is synced to the Hong Kong server. `project.php` now uses a more specific public listing title/description, and `includes/project_detail_template.php` now builds shorter project detail titles plus clean meta descriptions truncated to SEO-safe length. Detail titles also add the project year when available and safe, preventing same-name/different-year project title collisions. Production PHP lint passed for the template. Live verification checked `/project.php` plus all 34 project detail URLs: 0 length issues, 0 duplicate title groups and 0 duplicate description groups. Server backup: `/root/artdon_project_seo_template_backup_20260801_154500`; cache backups: `storage/page_cache_backup_project_seo_template_20260801_154500`, `storage/page_cache_backup_project_seo_year_20260801_160000`.
- Remaining public SEO metadata cleanup is complete. `downloads.php` now uses a shorter SEO title, and production DB SEO descriptions were shortened for About pages, Contact, three Solution detail pages, one product category title and two Retail application detail pages. Public text/content was not changed. Live verification checked the remaining 70 sitemap pages outside the already-audited product/detail/blog/project-detail batches: 0 title/description length issues, 0 duplicate title groups and 0 duplicate description groups. Code backup: `downloads.php.bak_remaining_meta_20260801_161500`; DB backups: `/root/artdon_remaining_meta_backup_20260801_161500.json`, `/root/artdon_retail_app_meta_backup_20260801_162000.json`; cache backups: `storage/page_cache_backup_remaining_meta_20260801_161500`, `storage/page_cache_backup_retail_app_meta_20260801_162000`.
- Website image standards are established and synced to the Hong Kong server. `includes/upload.php` now automatically converts new image uploads to WebP, normalizes filenames to English slug style, resizes by usage and enforces target limits: Hero/Banner 1920px and ≤250KB, Product 1600px and ≤150KB, Project 1920px and ≤200KB, Article/General 1600px and ≤150KB. `admin/media.php` displays the standard table, current selected standard, naming/ALT/Title guidance, and `admin/assets/admin.js` adds the 16:10 project crop option while defaulting Hero to 16:9, Product to 1:1 and Project to 16:10. Formal documentation lives at `docs/IMAGE_STANDARDS.md`. Production PHP lint passed for `includes/upload.php` and `admin/media.php`; server WebP conversion tests passed for banners, products and projects. Server backup: `/root/artdon_image_standards_backup_20260801_163500`.
- Existing uploaded content images have been safely migrated to WebP on the Hong Kong server without deleting old JPG/PNG originals. The batch scanned 1,850 JPG/PNG files under `uploads/website`, created/reused 1,850 WebP mappings, converted 1,846 files, reused 4 existing WebP files, had 0 conversion failures and replaced 2,511 normal DB references plus 4,874 JSON/log-style references. A follow-up fixed 19 pre-existing missing public solution-image references by pointing them to valid project/blog WebP images. Final verification: database public project/blog image-path audit returns no old JPG/PNG public paths; key pages (`/`, `/products.php`, `/project.php`, `/solutions-hospitality.php`, `/resources-blog.php`) have 0 old upload image extensions; broad live audit checked 84 pages with 0 page errors and 260 image URLs with 0 bad images, with sampled uploaded images showing 623 WebP and 0 old JPG/PNG. Old originals are retained as safety fallback. Backups/reports: `/root/artdon_upload_image_webp_refs_backup_20260801_171000.json`, `/root/artdon_upload_image_webp_report_20260801_171000.json`, `/root/artdon_remaining_upload_image_refs_backup_20260801_173200.json`, `/root/artdon_missing_solution_images_backup_20260801_174500.json`; cache backups include `storage/page_cache_backup_upload_webp_20260801_171000`, `storage/page_cache_backup_upload_webp_final_20260801_172800`, `storage/page_cache_backup_upload_webp_refs_final_20260801_173200`, `storage/page_cache_backup_missing_solution_images_20260801_174500`.
- Homepage carousel slide 2 was rotated 90° to the right on the Hong Kong server by creating a new WebP copy and updating the saved `hero` block to point to it. Original image was not overwritten. New image: `uploads/website/banners/2026/06/20260621_144342_05ad2cb27d25059b-right-90.webp` (1440×1920, 171,928 bytes). Backup: `/root/artdon_home_hero_rotate_backup_20260801_165423`; cache backup: `storage/page_cache_backup_home_slide2_rotate_20260801_165424`. Live verification confirmed the homepage HTML references the new image and the image URL returns HTTP 200.
- Website speed baseline pass 1 is synced to the Hong Kong server. `index.php` now keeps only the primary homepage CSS render-blocking, preloads secondary CSS without blocking first paint, adds async image decoding and low fetch priority to below-the-fold homepage images, and cache-busts the homepage CSS to `v=6.12.14`. `assets/css/artdon_home.css` now applies `content-visibility:auto` to below-the-fold homepage sections so the browser can defer rendering work. `docs/PERFORMANCE_STANDARDS.md` records the site speed standards for CDN, cache, lazy load, JS/CSS and images. Production PHP lint passed for `index.php`; live checks confirm the homepage and key CSS/JS URLs return HTTP 200, the homepage references the new CSS version, and lazy images carry async/low-priority attributes. Server backup: `/root/artdon_speed_baseline_backup_20260801_170809`. Google PageSpeed API verification could not complete on 2026-08-01 because Google returned daily quota exceeded.
- Security headers baseline is synced to the Hong Kong server. New `includes/security_headers.php` sends the shared header set, and both `includes/bootstrap.php` and `includes/public_cache.php` call it so normal PHP responses and cached public page HIT responses include the same policy. Headers now include CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, X-Permitted-Cross-Domain-Policies and existing HSTS. CSP includes `upgrade-insecure-requests` and `block-all-mixed-content` while allowing current first-party assets plus YouTube/Vimeo/OpenStreetMap/Google Maps compatibility. `docs/SECURITY_HEADERS.md` records the standard. Production PHP lint passed for all three touched include files; live checks on homepage, products page and admin login confirmed the full header set, cached homepage HIT responses confirmed the full header set, and homepage mixed-content scan found 0 `http://` resource URLs. Server backup: `/root/artdon_security_headers_backup_20260801_171319`; cache backup: `storage/page_cache_backup_security_headers_20260801_171346`. SecurityHeaders.com automated fetch was blocked by Cloudflare challenge, so final external grade should be manually verified in browser.
- Schema standards are unified and synced to the Hong Kong server. `includes/schema.php` now provides reusable helpers for Organization, WebSite, WebPage, BreadcrumbList, Product, FAQPage, Article, VideoObject and Project. Product detail pages now use the unified Product helper; FAQ, Blog listing, Blog detail and Videos pages now include Organization/WebSite/page context plus their specific FAQ/Article/Video schema; project detail pages now include a true `Project` node while retaining Article context. `docs/SCHEMA_STANDARDS.md` documents required Schema by page type and makes helper use the standard for future templates. Production PHP lint passed for `includes/schema.php`, `includes/project_detail_template.php`, `product.php`, `resources-videos.php`, `resources-faq.php`, `resources-blog.php` and `resources-blog-detail.php`. Live verification checked 12 representative pages (`/`, products list, series, product detail, FAQ, Blog listing, Blog detail, Videos, project listing, project detail, Hospitality solution and Retail application): all returned parseable JSON-LD with 0 parse errors and the expected Schema types. Server backups: `/root/artdon_schema_unified_backup_20260801_180540`, `/root/artdon_schema_blog_listing_backup_20260801_180910`; cache backups: `storage/page_cache_backup_schema_unified_20260801_180602`, `storage/page_cache_backup_schema_blog_20260801_180910`.
- Image standards verification item ① is complete and synced to the Hong Kong server. `product.php` now provides fallback ALT text for photometric curve images when the database ALT field exists but is empty; `includes/project_detail_template.php` and `includes/solution_page_template.php` provide non-empty fallback ALT text for project CTA and solution guide/CTA images. Production PHP lint passed. Live representative audit checked 9 key pages (`/`, `/products.php`, EMMA series, EMMA variant, `/project.php`, Zara project detail, `/resources-blog.php`, `/resources-videos.php`, `/solutions-hospitality.php`) with 291 images: `old_content_img_refs=0`, `missing_alt=0`, `bad_images=0`; 18 remaining PNG references are the brand logo exception. Server WebP size scan showed 0 over-limit files for banners, products, projects and general images. Server backups: `/root/artdon_image_alt_fix_backup_20260801_184622`, `/root/artdon_image_alt_fix2_backup_20260801_185013`; cache backups include `storage/page_cache_backup_image_alt_fix_20260801_184641` and the follow-up image-alt cache backup created during the second product.php deployment.
- Website speed verification item ② has been partially verified but should not be checked complete yet. Representative live pages (`/`, `/products.php`, EMMA series, EMMA variant, `/project.php`, Zara project detail, `/solutions-hospitality.php`) return HTTP 200 with gzip, 0 blocking JavaScript, one preloaded/high-priority first image where applicable and lazy loading for below-the-fold images. Response headers confirm page cache HIT on homepage, CSS/JS 30-day immutable caching and image/logo 180-day immutable caching. Two items remain unconfirmed/incomplete: Google PageSpeed API returned daily quota exceeded on 2026-08-01, so Mobile/Desktop scores could not be verified; DNS/response checks show `artdonlighting.com` and `www.artdonlighting.com` resolve directly to `43.132.210.162` with nginx/page-cache headers and no CDN layer marker, so CDN should be treated as not enabled unless Tencent/CDN configuration is added externally.
- Security verification item ③ is complete. Live audit checked 9 representative URLs including homepage, products list, EMMA series, EMMA variant, projects list, Zara project detail, Hospitality solution, resources blog and admin login. All returned the required security headers: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy and HSTS. The CSP includes `frame-ancestors`, `upgrade-insecure-requests` and `block-all-mixed-content`; weak-item count was 0 and active mixed-content resource count was 0. `includes/security_headers.php` remains the shared source for public and cached responses.
- Schema verification item ④ is complete and synced to the Hong Kong server. `resources-blog.php` was tightened so the Blog & Insights listing now emits a `CollectionPage`, retains a `Blog` node and includes Article summary nodes for the first visible posts. Production PHP lint passed. Live schema audit checked 12 representative pages covering homepage, product list, product family, product detail, FAQ, blog listing, blog detail, videos, project list, project detail, solution and retail application. All pages returned parseable JSON-LD with `parse_errors=0` and `missing_expected_pages=0`, covering Organization, Product/ProductGroup, FAQPage, Article, BreadcrumbList, VideoObject, Project, Service, CollectionPage, WebPage and WebSite as appropriate. Server backup: `/root/artdon_schema_blog_list_backup_20260802_111941`; cache backup: `storage/page_cache_backup_schema_blog_list_20260802_112005`.
- Content-system verification item ⑤ is partially complete but should not be checked complete yet. Program/backend capability is present: `admin/resources_blog_categories.php` can add, edit, reorder, hide and delete blog categories, and article deletion/moving is supported without losing posts. Production data currently has 78 published blog articles across 4 visible categories: `lighting-knowledge` 38 articles, `industry-news` 37, `artdon-news` 2 and `artdon-lighting-insights` 1. The live blog page shows these same 4 anchors. However, the Excel requirement was to establish a Knowledge / Insights / Case Study three-system content structure with weekly updates. `Insights` exists only as one article, `Case Study` does not exist on the blog page, and weekly ongoing updates cannot be validated as a completed one-time code task. Treat item ⑤ as not complete until a Case Study category/content plan is approved and ongoing publishing responsibility is assigned.
- Serious issue #1, project URL/content mismatch, is fixed and synced to the Hong Kong server. Project detail URLs now use `/projects/{slug}` via Nginx rewrite, and `project-detail.php?slug=...` / legacy wrong slugs redirect with HTTP 301 to the canonical pretty URL. `web_project_redirects` stores old-to-new slug mappings. The affected production project slugs were corrected, including `residential-villa -> jakarta-international-airport-indonesia`, `louvre-museum -> ferragamo-boutique-beijing-china`, `12345 -> lotte-shopping-mall-incheon-korea`, plus other early placeholder slugs (`zara-flagship-store`, `park-hyatt-hotel`, `google-office`, `apple-store`, `vitra-campus`, `mandarin-oriental-hotel`, `uniqlo-store`, `123`). The duplicate Lotte record was disambiguated to `lotte-shopping-mall-incheon-korea-2026` so the main requested URL can exist. `project.php` and project detail canonical/share/form URLs now output the pretty URL through `artdon_project_pretty_url()`, and `sitemap.php` now lists `/projects/...` with 0 `project-detail.php?slug=` entries. Verification: the three user-specified URLs return HTTP 200 with matching title/H1/canonical, the three old URLs return HTTP 301 to the correct pretty URLs, and all 34 sitemap project URLs return HTTP 200. Backups: `/root/artdon_project_url_fix_backup_20260802_113141`, `/root/artdon_project_slug_backup_20260802_113400.json`; cache backup: `storage/page_cache_backup_project_url_fix_20260802_113422`.
- Serious issue #2, project detail template application mismatch, is fixed and synced to the Hong Kong server. `includes/project_detail_template.php` now derives the bottom solution/CTA profile from project information, category, title and location instead of trusting stale per-project JSON text. Supported profiles cover Public Space / Transport, Retail, Hospitality, Office, Museum & Gallery, Residential and Commercial. Jakarta International Airport now displays Public Space / Transport Lighting copy and no longer contains the previous Retail phrases (`Explore our retail`, `Planning a Retail`, `boutiques`, `fashion stores`, `commercial retail`). Verification also checked Ferragamo retail, Croatian Hotel hospitality, Smart Office office, Museum Exhibition museum/gallery, Luxury Villa residential, Shanghai High-Speed Rail transport, Xiamen Exhibition Hall museum/gallery and Korea Business Center office; all rendered the expected profile. Server backup: `/root/artdon_project_application_template_backup_20260802_114145`; cache backup: `storage/page_cache_backup_project_application_template_20260802_114211`.
- Serious issue #3, homepage backend status text exposure, is fixed and synced to the Hong Kong server. The homepage product tab empty-state message `No product is published in this homepage tab yet.` was removed from public output along with its unused CSS/JS hooks, so the CMS/backend status text is no longer present even as hidden HTML. Production PHP lint passed for `index.php`; live homepage source verification returned `No product is published in this homepage tab yet.` count `0`, `artdon-v7153-empty` count `0`, and `artdon-v7153-card` count `37`, confirming products still render. Server backup: `index.php.bak_remove_home_empty_*`.
- Serious issue #4, Downloads page count/list mismatch, is fixed and synced to the Hong Kong server. Root cause was twofold: `artdon_v710_type_key()` treated the literal `all` value as unknown and converted it to `other`, and a stale/unsafe page-cache entry allowed the zero-result `type=other` view to appear as the default Downloads page. `includes/artdon_pages_v710.php` now preserves `all`, and `includes/public_cache.php` adds normalized GET parameters to the cache key so filtered pages cannot pollute unfiltered cache entries. Production PHP lint passed for both files; old page cache was cleared. Live verification: `/downloads.php` and `/downloads.php?type=all` show `165 files found` with 36 visible rows on page 1 and no empty message; `?type=datasheet` shows 27 rows; `?type=photometric` shows 138 found with 36 rows on page 1; `?type=other` correctly remains 0. Cache HIT retest confirmed visiting `type=other` no longer affects the default Downloads page. Server backups: `includes/artdon_pages_v710.php.bak_download_type_all_*`, `includes/public_cache.php.bak_download_cache*`.
- Serious issue #5 (empty product categories in main navigation) was explicitly skipped by the user for now: "这个先忽略 下一个问题". Do not fix/hide Linear Lighting or LED Strips & Profiles until the user asks again.
- Serious issue #6, inconsistent company years, is fixed and synced to the Hong Kong server. Front-end/default content and saved production DB content were normalized away from conflicting `17+`, `18+` and `19+ Years Experience` claims toward stable `Since 2007` wording. Updated files: `index` defaults via `includes/default_content.php`, About pages, About data defaults, Solutions defaults and relevant admin placeholders/defaults. Production database blocks/pages were backed up to `/root/artdon_since2007_db_backup_20260802_121026.json` and updated in 4 saved records; page cache was cleared. Production PHP lint passed for all 9 deployed files. Live representative audit checked `/`, `/about.php`, `/about-why-artdon.php`, `/about-manufacturing.php`, `/solutions.php` and `/solutions-hospitality.php`: all returned 0 counts for `17+`, `18+`, `19+`, `Years Experience`, `Years of Experience`, `18+ years` and `19+ Years`. Production DB residual check also returned 0 matches across `web_content_blocks`, `web_about_pages` and `web_solution_pages`. Server file backup: `/root/artdon_since2007_backup_20260802_120920`.
- Serious issue #7, Blog template/content pollution, is fixed and synced to the Hong Kong server. The default blog-detail content generator in `includes/resources_blog_data.php` was changed from a Beam Angle-specific fallback to a generic Lighting Insight fallback so future empty/new fallback articles do not inherit unrelated Beam Angle copy. Production database audit found 12 published non-Beam-Angle articles containing the sentence `Beam angle describes the spread of light emitted from a luminaire...`; all 12 were backed up and cleaned. The UGR article's duplicate `card_grid_title` of `Key Takeaways` was renamed to `Glare Control Essentials`, leaving only the real top Key Takeaways block. DB backup: `/root/artdon_blog_template_pollution_backup_20260802_121630.json`; file backup: `/root/artdon_blog_template_fix_backup_20260802_121641`. Production PHP lint passed for `includes/resources_blog_data.php`, cache was cleared. Verification: DB scan across 78 published blog articles returned `beam_pollution=0` and `duplicate_takeaways=0`; live UGR page has 0 Beam Angle pollution sentence, 1 top `KEY TAKEAWAYS`, 0 duplicate `Key Takeaways`, 1 `Glare Control Essentials`, and 0 `Common Beam Angles` / `What Is Beam Angle?` headings.
- Serious issue #8, product series application content being highly templated, is fixed as a first production cleanup pass. Production DB audit found 23 product families whose saved `family_applications_json` contained the repeated phrase `visual comfort, reliable performance and a consistent architectural appearance` and/or the wrong retail point `Supports product presentation and customer circulation`. All affected rows were backed up to `/root/artdon_product_applications_template_backup_20260802_122138.json` and updated directly in production; page cache was cleared. ARMI and ARTAX received hand-written application cards: ARMI now references concealed/recessed track integration, articulated aiming, luxury retail, flagship zones, premium commercial interiors and boutique displays; ARTAX now references museum/gallery/exhibition/cultural-space beam control, high CRI, low glare and visitor viewing comfort, with no retail-language points in Cultural Space. Other affected series received rule-based application-specific copy by scene/product type. Verification: production DB residual scan returned `remaining=0`; live ARMI, ARTAX and EMMA pages all returned 0 counts for the template phrase and wrong retail point. Live ARMI contains the new luxury retail sentence; live ARTAX contains the new museum sentence and the corrected cultural-space sentence.
- ARMI was then upgraded as the first deeper expert-content sample for product application rewriting. Only the ARMI series `family_application_intro` and four saved application cards were updated in production; images, product parameters and page structure were not changed. The new ARMI copy adds designer-facing selection detail: recommended mounting height around 2.7–4.0 m, close/medium aiming distances, IES/LDT confirmation for spot size, accent-vs-ambient lighting limitation, difference from standard exposed track, recessed track/ceiling coordination requirement, dimming/control notes and accessory/driver coordination. DB backup: `/root/artdon_armi_expert_application_backup_20260802_123326.json`. Live ARMI verification returned 1 count each for the new expert phrases (`concealed accent-lighting system`, `Recommended mounting height: about 2.7–4.0 m`, `confirm beam size with IES/LDT files`, `Different from standard track`, `requires planned recessed track/ceiling coordination`) and 0 counts for the old template phrase / wrong retail point. Await user review before applying the same depth to other series.
- After the user confirmed they could see the ARMI changes, ARTAX was upgraded as the second deeper expert-content sample. Only ARTAX `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. The new ARTAX copy positions it as a projection-style accent system for museums, galleries, exhibitions and cultural spaces, with designer-facing notes on recommended mounting height about 3.0–5.0 m, close/medium target distance, final illuminance verification through IES/LDT, 20°–35° / 25°–45° distribution as a starting point, shutters/gobos/adjustable focus, difference from standard accent spotlight and wall washer, DALI/scene control, accessories/driver/control coordination and commissioning limits. DB backup: `/root/artdon_artax_expert_application_backup_20260802_124140.json`. Live ARTAX verification returned 1 count each for the new expert phrases (`projection-style accent system`, `Recommended mounting height: about 3.0–5.0 m`, `verify final illuminance with IES/LDT files`, `20°–35° / 25°–45°`, `not intended for uniform wall washing`, `confirm shutters, gobos, lenses...`, `requires coordinated track location and commissioning time`) and 0 counts for the old template phrase / wrong retail point.
- EMMA was upgraded as the third deeper expert-content sample. Only EMMA `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. The new EMMA copy positions it as a compact architectural track-light family for fashion retail, restaurants, hospitality and commercial interiors, with designer-facing notes on recommended mounting heights (about 2.7–4.2 m for fashion retail, about 2.6–3.6 m for restaurants), beam selection starting points (19°/29° for mannequins and hero displays, 44°/56° for broader bays/tables), seated-guest glare strategy, TRIAC vs DALI scene-control notes, Ø45/Ø62 vs Ø73/Ø90 size logic, limitation vs linear general office lighting, and track/finish/driver/dimming/IES-LDT coordination. DB backup: `/root/artdon_emma_expert_application_backup_20260802_124501.json`. Live EMMA verification returned 1 count each for the new expert phrases (`compact architectural track-light family`, `Recommended mounting height: about 2.7–4.2 m`, `19°/29° for mannequins and hero displays`, `keep beam direction away from seated guests`, `Ø45/Ø62 for compact interiors`, `not a replacement for linear general office lighting`) and 0 counts for the old template phrase / wrong retail point.
- Four additional priority Track Light families were upgraded in the same deeper expert-content style: SPECTRUM Track Light, HIGH LUMEN PRO Track Light, BEAMX Track Light and ECO Track Light. Only `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. DB backup: `/root/artdon_track_priority_expert_applications_backup_20260802_125044.json`. SPECTRUM now covers modular optics/accessories, 20°/25°/39°/51° beam-start logic, anti-glare louvers/barn doors, SPECTRUM vs ARTAX/ECO selection differences and mall/gallery/display limitations. HIGH LUMEN PRO now covers high-ceiling/large-space use, airport/exhibition/mall applications, about 4.0–7.0 m or 3.8–6.0 m mounting guidance, longer throw, DALI zones, high-output glare risk and alternative choices for compact/lower ceilings. BEAMX now covers narrow high-precision accent use for jewellery, museum, gallery and luxury displays, about 2.6–3.5 m mounting guidance, close throws, sparkle/hotspot/glass-counter glare, and differences vs ARTAX / EMMA / SPECTRUM. ECO now covers value-driven chain stores, supermarkets, retail and general commercial interiors, about 2.8–4.0 m mounting guidance, 22°/27°/38°/56° beam-start logic, rollout repeatability, and limitations vs SPECTRUM / EMMA / HIGH LUMEN PRO. Live verification for all four pages returned 1 count for each new intro and mounting-height phrase, and 0 counts for the old template phrase / wrong retail point.
- Six priority Downlight families were upgraded in the same deeper expert-content style: INTERO, SLIM, SILO, FLEXI, OPTIMAX and SOFT. Only `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. DB backup: `/root/artdon_downlight_priority_expert_applications_backup_20260802_125649.json`. INTERO now covers adjustable high-output recessed accent use, 2.8–4.2 m retail mounting guidance, 29°/35° vs 47°/58° beam logic, recessed-vs-track limitations and ceiling closure coordination. SLIM now covers fixed high-output downlighting, 2.8–4.0 m retail and 2.7–3.6 m office mounting guidance, fixed spacing, general-area calculation and limitations vs adjustable/track/high-ceiling products. SILO now covers luxury hotel/villa/high-end residential use, 2.6–3.6 m hotel and 2.6–3.4 m villa guidance, ultra-thin trim/CNC/removable-module advantages, dimming scenes and residential glare/service notes. FLEXI now covers modular fixed/adjustable/wall-washer recessed downlight use, 2.6–3.8 m hotel and 2.6–3.5 m villa guidance, 55/75/100 mm cut-out coordination, wall-washer offset and track-vs-recessed integration differences. OPTIMAX now covers practical commercial/office/retail use, 2.7–3.6 m office guidance, Ø50/Ø65 cut-out logic, thermal management, work-plane verification and limitations vs high-ceiling/museum-grade accent. SOFT now covers ultra-low-glare fixed downlight use, UGR<3 comfort positioning, 2.6–3.4 m hotel and 2.7–3.5 m office guidance, dimming/low-brightness scenes, non-removable module service note and limitations vs directional accent/wall grazing. Live verification for all six pages returned 1 count for each new intro and mounting-height phrase, and 0 counts for the old template phrase / wrong retail point.
- Five Low Voltage / Magnetic System families were upgraded in the same deeper expert-content style: SPECTRUM Low Voltage Track Light, EMMA Low Voltage Track Light, BEAMX Low Voltage Track Light, MAGENTRA Low Voltage Linear Light Ultra-Anti-Glare and MAGENTRA Low Voltage Linear Light Wall Washer. Only `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. DB backup: `/root/artdon_low_voltage_expert_applications_backup_20260802_133412.json`. SPECTRUM LV now covers compact low-voltage accent layouts, 2.6–3.8 m interiors, boutique/mall/gallery/residential use, driver/track load coordination and limitations vs high atrium/long-throw applications. EMMA LV now covers compact hospitality, fashion-retail and residential use, 2.5–3.5 m interiors, seated-guest glare, dimming scenes and accent-vs-ambient limitations. BEAMX LV now covers jewellery, museum, gallery and small luxury-display use, close-range precision beams, reflection/glare checks and limits vs wall washing/high-ceiling lighting. MAGENTRA Ultra Anti-Glare now covers comfortable linear base lighting for hospitality, residential, office and retail circulation. MAGENTRA Wall Washer now covers vertical retail walls, museum/gallery walls, hospitality corridors and residential feature walls, including early layout guidance around 0.4–0.8 m wall offset for many 2.7–4.0 m spaces. Live verification for all five `/products/magnetic-systems/...` pages returned HTTP 200 and 0 counts for the old template phrase / wrong retail point.
- Eight Surface/Pendant and remaining MAGENTRA linear families were upgraded in the same deeper expert-content style: UTH Surface Mounted Light, TLA Surface Mounted Light, WASHO Surface Mounted Light, ADJ Surface Mounted Light, SPECTRUM Pendant Light, DELTA Pendant Light, MAGENTRA Low Voltage Linear Light Microprismatic and MAGENTRA Low Voltage Linear Light Anti-Glare. Only `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. DB backup: `/root/artdon_surface_pendant_magentra_expert_backup_20260802_134118.json`. UTH/TLA now cover surface-mount use where recessed installation is limited, practical mounting-height guidance, glare/screen/sightline checks and hospitality/residential/office use. WASHO now covers surface-mounted wall washing for retail, museum/gallery, hotel feature walls and material showrooms. ADJ now covers surface-mounted adjustable accent use for luxury retail, galleries, residential accents and premium showrooms. SPECTRUM/DELTA Pendant now cover high-ceiling suspended accent logic, stem height, sightlines, IES/LDT checks and restaurant/hotel/retail/gallery use. MAGENTRA Microprismatic/Anti-Glare now cover office task, retail base, hospitality, residential and gallery-support lighting without the previous residential/office mismatch. Live verification for all eight pages returned HTTP 200 and 0 counts for old template/mismatched phrases.
- Nine remaining Downlight/Cabinet families were upgraded in the same deeper expert-content style: LITE PRO, HICORE, VERO, VOLI, MINI PRO, MINI, SHOWLITE, NOVAL and ULTRA-SLIM. Only `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. DB backup: `/root/artdon_remaining_downlight_cabinet_expert_backup_20260802_134809.json`. LITE PRO now covers practical hotel/villa/office/high-end residential fixed recessed use; HICORE covers higher-output airport/exhibition/mall/public-building use around 3.5–6.0 m; VERO covers refined luxury residential/hotel/café/dining use; VOLI covers compact fixed retail/hospitality/gallery-support/office use; MINI PRO covers compact serviceable hotel/apartment/corridor use; MINI covers small residential/cabinet/boutique/feature accent use; SHOWLITE covers jewellery, museum showcase, luxury retail and display cabinet lighting; NOVAL covers compact adjustable retail/fashion/shoe boutique/dining accents; ULTRA-SLIM covers cabinet, wardrobe, showcase and display furniture integration. A residual MINI PRO feature phrase containing `professional lighting performance` was also replaced; backup: `/root/artdon_mini_pro_feature_phrase_backup_20260802_135159.json`. Live verification for all nine `/products/downlights/...` pages returned HTTP 200 and 0 counts for the old template phrases.
- WASHO Track Light plus two outdoor downlight families were upgraded as the product-content cleanup closeout pass: WASHO Track Light, VOLI IP65 and SOFT IP65. Only `family_application_intro` and the four application cards were updated in production; images, parameters and page structure were not changed. DB backup: `/root/artdon_washo_outdoor_expert_backup_20260802_135530.json`. WASHO Track now covers dedicated wall-washing use, wall offset / mounting-height planning, IES/LDT verification, retail walls, paintings, feature walls and decorative-object limitations vs projection-style accents. VOLI IP65 and SOFT IP65 now cover sheltered outdoor ceiling use with IP65 sealing, drainage, maintenance access, glare control, dimming, hotel entrance canopies, villa terraces, covered walkways and outdoor dining. Follow-up database residual scan across published product `family_application_intro` and `family_applications_json` returned `remaining=0` for tracked template phrases. Live verification for all three pages returned HTTP 200 and 0 old-template counts.
- Mobile navigation was fixed for the reported issue where the phone menu could open mid-list and Products were not visible. `assets/js/artdon_home.js` now resets `siteNav.scrollTop=0` whenever the menu opens, toggles `body.nav-menu-open`, restores the state on close and supports Escape close. `assets/css/artdon_home.css` now gives the open mobile menu a fixed full-screen scroll area below the header, raises its z-index above page/floating UI, hides the floating quick-action bar while the menu is open, prevents background scrolling and adds bottom safe-area padding so Contact/Get a Quote are not covered. CSS was cache-busted to `v=6.12.15` and JS to `v=6.12.14` across public templates/pages. Server backup: `/root/artdon_mobile_menu_backup_20260802_1400`. Production PHP lint passed for all touched PHP pages/templates; cache was cleared. Live checks confirmed the homepage references the new CSS/JS versions, the mobile menu HTML contains Products directly after Home plus all product categories, and live CSS/JS contain the new `nav-menu-open`, floating-action hide and `siteNav.scrollTop=0` rules.
- Mobile homepage issue #1 (homepage too long / difficult to grasp) is fixed as a mobile-only focus pass. Desktop layout/content remains unchanged. `index.php` now adds a mobile “View all product families” CTA after the homepage product grid and limits visible homepage product cards to 8 on screens up to 760px, while keeping desktop unlimited. `assets/css/artdon_home.css` adds the V6.12.16 mobile homepage focus rules: hide secondary/long desktop-only sections (`home-partner-reasons`, `home-clean-system`, `home-download-hub`, `home-insights`) on mobile, shorten the hero text, reduce vertical padding, show only the first four trust cards, first two projects and first four solution cards, compress product cards to a two-column mobile grid, and hide the full homepage inquiry form on mobile so the section becomes a shorter contact/inquiry entrance. CSS was cache-busted to `v=6.12.16` across public templates/pages. Server backup: `/root/artdon_mobile_home_short_backup_20260802_1415`. Production PHP lint passed for all touched PHP pages/templates, cache was cleared, and live checks confirmed the homepage references CSS `v=6.12.16`, contains the new `mobile-home-section-more` CTA and `mobileLimit()` logic, and the live CSS contains the mobile focus rules.
- Mobile homepage issue #2 (first-screen copy looked too much like a China manufacturer/OEM introduction) is fixed directly in the production `hero` content block. Only homepage slide 1 text was changed; slides 2 and 3 were not changed. New slide 1 eyebrow: `ARCHITECTURAL PROJECT LIGHTING`; new visual title: `Architectural Lighting. Engineered for Projects.`; new description: `Manufacturer of professional track lights, downlights and custom lighting systems since 2007.` This keeps SEO/product keywords while making the first visual impression more brand/project oriented. DB backup: `/root/artdon_mobile_hero_copy_backup_20260802_142604.json`. Page cache was cleared. Live verification confirmed the new title and description appear once on the homepage, the old `Architectural Lighting Manufacturer in China` visual title is gone, and the page SEO title/description still retain manufacturer, track lights, downlights, magnetic systems and OEM/ODM keywords.
- Mobile product-list issue #3 (filter list too long / hard to operate on phone) is fixed and synced to the Hong Kong server. `products.php` now adds a mobile-only filter backdrop, close button and bottom Reset / Show products action bar; `assets/css/artdon_products_inline_v718.css` turns the product filters into an 82dvh bottom sheet on screens up to 800px while preserving the approved desktop left filter; `assets/js/artdon_home.js` stops automatic checkbox/radio form submission on mobile, opens/closes the bottom sheet cleanly, hides background scroll, supports Escape close and updates the Show count label after filter changes. CSS was cache-busted to `v=7.1.8.185` and product-page JS to `v=6.12.15`. Server backups: `/root/products_mobile_filter_backup_*`, `/root/artdon_home_js_mobile_filter_backup_*`, `/root/artdon_products_css_mobile_filter_backup_*`. Production PHP lint passed for `products.php`, page cache was cleared, and live source verification confirmed the new mobile filter markup plus updated CSS/JS versions.
- Mobile menu priority adjustment is fixed and synced to the Hong Kong server. When the phone menu opens, the shared navigation now automatically scrolls to the Products section instead of staying/restoring near the bottom; customers can then scroll manually to About, Resources and Contact if needed. `partials/header.php` marks the Products navigation block with `data-mobile-menu-products`, and `assets/js/artdon_home.js` uses that marker to reposition the mobile menu on open, including a delayed second pass for mobile browser layout restore quirks. Public JS was cache-busted to `v=6.12.16` across public PHP pages/templates. Production PHP lint passed for `partials/header.php`, `index.php` and `products.php`; page cache was cleared; live homepage verification confirmed `data-mobile-menu-products`, JS `v=6.12.16` and `scrollMobileNavToProducts` are present. Server backup: `/root/artdon_mobile_nav_products_backup_*`.
- Mobile product-list issue #4 (zero-result filter options) is fixed and synced to the Hong Kong server. `products.php` now omits unselected filter options whose `usage_count` is 0, while preserving a selected zero-count option long enough for the user to cancel it. The live filter controller also hides options that become zero-count during client-side filtering, and `assets/css/artdon_products_inline_v718.css` now hides `.is-zero-count` labels instead of merely fading them. Product filter CSS was cache-busted to `v=7.1.8.186`. Production PHP lint passed for `products.php`; page cache was cleared; live `/products.php` verification confirmed CSS `v=7.1.8.186`, no `<small>0</small>` filter options in the rendered source, and the zero-hide code present on the server. Server backup: `/root/artdon_filter_zero_hide_backup_*`.
- Mobile menu Products priority was refixed after the user reported it still did not land on the current screen. The second fix is more forceful: `partials/header.php` now marks the actual `All Products` link with `data-mobile-menu-all-products`; `assets/js/artdon_home.js` targets that link first and retries repositioning at 80/220/520ms after opening; `assets/css/artdon_home.css` gives the Products nav group `order:-20` when the mobile menu is open, so Products appears first even if the browser restores an old menu scroll position. Public CSS/JS were cache-busted to `v=6.12.17` across public PHP pages/templates. Production PHP lint passed for `partials/header.php` and `index.php`; page cache was cleared; live homepage verification confirmed `data-mobile-menu-products`, `data-mobile-menu-all-products`, CSS `v=6.12.17`, JS `v=6.12.17`, the CSS order rule and JS reposition logic. Server backup: `/root/artdon_mobile_nav_products_refix_backup_*`.
- Mobile menu Products priority final root-cause fix is synced to the Hong Kong server after browser-based mobile testing showed Products was still above the visible viewport. Root cause was an older mobile nav rule leaving `.site-nav.open` with `justify-content:flex-end`, causing over-tall menu content to bottom-align and push Products off-screen even when `scrollTop=0`. `assets/css/artdon_home.css` now forces `.site-nav.open{justify-content:flex-start!important;}` while keeping Products ordered first and the All Products JS target. Public CSS/JS were cache-busted to `v=6.12.18` across public PHP pages/templates. Production PHP lint passed for `partials/header.php` and `index.php`; page cache was cleared; live source verification confirmed CSS/JS `v=6.12.18`, Products/All Products markers and the flex-start rule. In-app browser mobile viewport test at 390×844 confirmed: `justifyContent=flex-start`, Products top `20`, All Products top `76`, and the first visible menu items are Products, All Products, Track Lights, Downlights and other product categories. Server backup: `/root/artdon_mobile_nav_flexstart_backup_*`.
- Mobile product-list issue #5 (product cards too dense on phones) is fixed and synced to the Hong Kong server. `products.php` now marks each card spec row by type and adds a mobile-only card CTA (`View Series →` / `View Product →`). `assets/css/artdon_products_inline_v718.css` adds the mobile card simplification rules: 4:3 image area, tighter body spacing, one-line subtitle, only Wattage / Size / Beam Angle visible, Lumen Output and Best For hidden on mobile, tags limited to the first two, and the mobile CTA displayed as a clear pill button. Product filter/card CSS was cache-busted to `v=7.1.8.188`. Production PHP lint passed for `products.php`; page cache was cleared. In-app browser mobile viewport test at 390×844 confirmed the first product card loads CSS `v=7.1.8.188`, visible rows are only `Wattage`, `3 Sizes` and `Beam Angle`, hidden rows are `Lumen Output` and `Best For`, and `View Series →` displays. Server backup: `/root/artdon_mobile_card_simplify_backup_*`.
- Mobile product-detail/series issue #6 (decision information appears too late on phones) is fixed and synced to the Hong Kong server for product family pages such as ARMI and FLEXI. Desktop storytelling order is unchanged. `series.php` now adds mobile-only ordering rules (`V7.1.8.189`) so phone screens display the sequence: Hero overview → `Choose the right size` / variants → Downloads → Get Quote/Support → Why/Characteristics/Applications/Projects/Related. Production PHP lint passed for `series.php`; page cache was cleared. In-app browser mobile viewport test at 390×844 on `/products/track-lights/armi-track-light` confirmed section order by rendered top position: `overview` order 1, `products` order 2 with heading `Choose the right size` and 3 variant cards, `downloads` order 3, then `why`, `characteristics`, `applications` and `projects`. Server backup: `/root/series_mobile_decision_order_backup_*`.
- Mobile product-detail/series issue #7 (`Choose the right size` should work as a quick size selector) is fixed and synced to the Hong Kong server. `series.php` now marks variant spec rows (`s717-spec-power`, `s717-spec-size`, `s717-spec-lumen`, `s717-spec-beam`) and adds a mobile-only `View details →` action on each variant card. Mobile-only `V7.1.8.190` CSS turns the variant list into compact quick-selection cards with a 112px image column, model name, Wattage/Size/Beam Angle only, Lumen hidden, first two tags max, accessory images collapsed into an accessories note, and a clear detail CTA. Production PHP lint passed; page cache was cleared. In-app browser mobile viewport test at 390×844 on ARMI confirmed the first card renders as a grid (`112px 248px`), title `ARMI 45`, visible rows `Wattage: 11W`, `Size: Ø45*80mm`, `Beam Angle: 19°/29°/44°/56°`, hidden `Lumen Output`, hidden accessory list, and visible `View details →`. Server backup: `/root/series_mobile_quick_size_backup_*`.
- Mobile product-detail issue #8 (Technical Downloads unclear on phone) is fixed and synced to the Hong Kong server. `product.php` now gives download items clearer labels and metadata (`Datasheet`, `IES / LDT`, `Product image`, `Dimension drawing`, plus format/English/file size or generated-PDF note), and the section title is now `Technical Downloads`. `assets/css/artdon_product_inline_v718.css` adds mobile file-card styling with readable file labels and a compact black `Download ↗` pill; product detail CSS was cache-busted to `v=7.1.8.190`. Production PHP lint passed for `product.php`; page cache was cleared. In-app browser mobile viewport test at 390×844 on `/products/track-lights/armi-track-light/32.07326` confirmed CSS `v=7.1.8.190`, heading `Technical Downloads`, 4 download cards, metadata `PDF · EN · Generated PDF`, `ZIP · EN · 771 KB`, `WEBP · EN · 9 KB`, `WEBP · EN · 8 KB`, and the button renders black background with white text.
- Mobile product menu category search follow-up is fixed and synced to the Hong Kong server. Phone users who open the menu and tap a product category such as `Track Lights` now land on `/products.php?category=track-lights`, preserving the unified Products search/filter interface instead of losing search on a category-style page. `partials/header.php` adds mobile-only product-filter targets to product category links; `assets/js/artdon_home.js` intercepts those targets only on screens up to 900px; `products.php` renders a mobile-only search form on category-intro pages; `assets/css/artdon_products_inline_v718.css` keeps that search hidden on desktop and visible on mobile. Public JS was cache-busted to `v=6.12.19`; Products CSS was cache-busted to `v=7.1.8.191`. Production PHP lint passed for `partials/header.php` and `products.php`; page cache was cleared. In-app browser tests confirmed at 390×844 tapping mobile menu `Track Lights` opens `https://artdonlighting.com/products.php?category=track-lights` with `Search products` visible and hidden category `track-lights`; at 1366×900 the mobile-only category search remains `display:none`.
- Mobile inquiry issue #9 (too many inconsistent inquiry entrances) has a first mobile-only unification pass live. `includes/floating_actions.php` now treats mobile product and project CTAs as one shared inquiry workflow: `Get a Quote` / `Request a Quote` normalize to `Request a Quote`, and `Discuss ... Project` / `Project Inquiry` / `Talk to Our Engineers` normalize to `Discuss a Project`. On mobile, those CTAs open the shared floating inquiry modal instead of jumping to Contact or opening a page-specific modal; product clicks save `support_type=quotation`, project/solution clicks save `support_type=project`, and the relevant product/model/project plus current link are still auto-filled. Production PHP lint passed for `includes/floating_actions.php`; page cache was cleared. In-app browser mobile test at 390×844 confirmed product page `ARMI 73` shows `Request a Quote`, opens the shared modal with `ARMI 73 / 32.07326` and `supportType=quotation`; Hospitality solution shows `Discuss a Project →`, opens the same shared modal with `Hospitality Lighting Project` and `supportType=project`, while the page-specific `heroQuoteModal` stays closed.
- Mobile inquiry issue #10 (phone inquiry form too long) is fixed for the shared inquiry modal. `includes/floating_actions.php` now keeps desktop unchanged but simplifies the phone form to the first-step fields: `Name`, `Email or WhatsApp`, `WhatsApp`, `Country` and `Project requirement`; `Company` remains in the form for desktop but is hidden on mobile as an optional follow-up field. The modal also keeps automatic product/model/project and URL hidden fields from issue #9. Production PHP lint passed for `includes/floating_actions.php`; page cache was cleared. In-app browser tests confirmed at 390×844 the visible phone fields are `name`, `email` (`aria-label=Email or WhatsApp`), `whatsapp`, `country`, `message`, with `company` hidden; at 1366×900 `company` still displays and email aria label remains `Email`.
- Mobile contact issue #11 (two WhatsApp numbers / unclear primary contact) is fixed and synced to the Hong Kong server. The primary WhatsApp is now `+86-13925332972` / `https://wa.me/8613925332972`. `includes/floating_actions.php` adds a phone-only `Chat with Artdon` WhatsApp action to the floating action bar and hides `Copy link` on phones, while desktop keeps `Copy link` and hides the WhatsApp action. `partials/footer.php`, `includes/footer.php`, `includes/default_content.php` and `includes/contact_page_data.php` now use `WhatsApp` + the single primary number. Production saved backend blocks were backfilled too: `site.mobile`, `site.whatsapp`, `footer.contact.mobile_label`, `footer.contact.mobile_value`, `footer.contact.whatsapp_number` and Contact page WhatsApp item text/url. Production PHP lint passed for all touched PHP files and page cache was cleared. In-app browser test at 390×844 on Contact confirmed floating actions are Inquiry / Email / Chat with Artdon on WhatsApp, Copy link is hidden, footer and Contact info both link to `https://wa.me/8613925332972`, and the old second number `137 5171 0245` is absent. Desktop test at 1366×900 confirmed WhatsApp floating action is hidden and Copy link remains visible.
- Mobile Solution issue #12 (Solution pages feel too dense on phones) is fixed and synced to the Hong Kong server. `assets/css/solution-detail.css` V1.0.15 adds mobile-only guided-reading rules for solution detail pages: the sticky tabs become a horizontal pill step bar below the 66px mobile header; section heads gain numbered labels (`01 / Problems`, `02 / Design goals`, `03 / Recommended products`, `04 / Applications & projects`, `05 / Project support`); challenge/support cards become compact icon+text list cards; design-guide cards are boxed; applications/projects become thumbnail list rows; recommended products get a `Swipe to compare products →` hint. `includes/solution_page_template.php` cache-busts `solution-detail.css` to `v=1.0.15`. Production PHP lint passed for the template and page cache was cleared. In-app browser mobile test at 390×844 on Hospitality confirmed CSS `v=1.0.15`, sticky tab top `66px`, tabs overflow-x `auto`, numbered pseudo-labels, one-column challenge/support grids, application rows as `118px + text`, and the swipe hint. Desktop test at 1366×900 confirmed the mobile numbering/hint/list layout does not apply.
- `contact.php`, `includes/default_content.php`, `includes/footer.php` and `includes/contact_page_data.php` are synced to the Hong Kong server with unified contact defaults and corrected Contact-page JSON-LD. Production PHP lint passed for all four files. Saved backend blocks `site`, `footer` and `contact_page_settings` were updated directly in production after backup.
- Product/content cleanup was applied directly in the Hong Kong production database. No source-code deployment was required beyond syncing this context file. Public checks confirm 0 NO IMAGE strings on the homepage/products page and 0 published duplicate/empty/no-image product records.
- SEO title and description data has been backfilled directly in the Hong Kong production database. The affected public SEO-capable tables now verify with 0 empty titles, 0 empty descriptions and 0 duplicate title groups among published/active records. No source-code deployment was required for this item beyond syncing this context file.
- Nginx vhost and certificate are live with canonical apex-domain routing. `www.artdonlighting.com` no longer serves the default site; it redirects to `https://artdonlighting.com` and the certificate validates with SANs for both apex and www.
- `index.php`, `products.php`, `series.php`, `product.php`, `Home/Products/index.php` and `includes/artdon_pages_v710.php` are synced to the Hong Kong server with canonical URL redirects and the updated products sitemap/canonical entry. Production PHP lint passed, legacy URL checks return 301, followed final URLs return HTTP 200, and the dynamic sitemap has 0 `/Home/Products` entries.
- `robots.txt`, `sitemap.xml`, `sitemap.php` and `includes/artdon_pages_v710.php` are synced to the Hong Kong server. The public robots file and sitemap-index URL both return HTTP 200 and reference `https://artdonlighting.com/sitemap.xml` / `https://artdonlighting.com/sitemap.php` as intended. All 380 generated public URLs were checked after deployment and return final 2xx responses.
- `admin/resources_blog_categories.php`, `admin/resources_blog.php`, `admin/save_resources_blog.php`, `admin/resources_blog_template.php`, `admin/resources_pages.php`, `admin/_layout.php`, `includes/resources_blog_data.php` and `resources-blog.php` are synced to the Hong Kong server. Blog categories now live in `web_resource_blog_categories`; the original Lighting Knowledge, Industry News and Artdon News categories were backfilled unchanged. PHP lint passed for all updated files, checksums match local, and the live blog page returns HTTP 200 with all three original category anchors. The new admin route is `admin/resources_blog_categories.php` and returns its expected login redirect when unauthenticated.
- `admin/settings.php`, `includes/bootstrap.php`, `includes/content.php`, `includes/default_content.php` and `assets/img/favicon-artdon.png` are synced to the Hong Kong server. The browser-tab icon is injected once into every public page head, defaults to the supplied Artdon A mark, and can be replaced at `后台 → 网站设置 → 浏览器标签图标（favicon）`. Production PHP lint passed, the site setting was backfilled, and the live Hospitality page returns the expected icon tag and image HTTP 200.
- GitHub push connectivity was restored permanently for normal Git commands by removing the broken `ProxyCommand nc -X 5 -x 127.0.0.1:7897 %h %p` line from the local `Host github.com` SSH config. Backup was saved as `~/.ssh/config.bak_github_no_proxy_20260801_133924`. Ordinary `ssh -T git@github.com` authenticates successfully and ordinary `git ls-remote origin refs/heads/main` works.
- `includes/retail_application_data.php` and `includes/solution_page_template.php` are synced to the Hong Kong server with public-to-admin Solution Application backfill. Both passed production PHP lint and checksums match local; the live Hospitality page returns HTTP 200 and database counts verify content in all six categories.
- `admin/_layout.php`, `admin/retail_applications.php`, `admin/retail_applications_action.php`, `admin/save_retail_application.php`, `includes/retail_application_data.php`, `includes/retail_application_template.php` and `solutions-application.php` are synced to the Hong Kong server with six parent Solution Application categories. All changed files passed production PHP lint and checksums match local; the original retail generic application route returns HTTP 200 and the empty new-category route returns its expected HTTP 404 until an application is created.
- `admin/retail_applications.php`, `admin/retail_applications_action.php`, `includes/retail_application_data.php` and `solutions-retail-application.php` are synced to the Hong Kong server with dynamic Retail Application create/delete support; all four passed production PHP lint and checksums match local. The generic public route was checked live with the existing Fashion Store application.
- `project.php`, `admin/project_action.php` and `admin/project_details.php` are synced to the Hong Kong server with editable public Projects banner image, ALT, large title and small descriptive copy; all three passed production PHP lint and their checksums match local.
- `series.php` is synced to the Hong Kong server with the shared 16:9 center-crop rule for all series Projects images; production PHP lint passed and checksum matches local.
- `series.php`, `product.php`, `includes/product_hierarchy.php`, `includes/product_accessories.php`, `admin/product_variant_edit.php`, `admin/products.php`, `admin/assets/admin_v7.css` and `assets/css/artdon_product_inline_v718.css` are synced to the Hong Kong server; all changed PHP files passed production lint and all eight deployed file checksums match local.
- `series.php`, `product.php`, `assets/js/artdon_product_inline_v718.js` and `assets/css/artdon_product_inline_v718.css` are synced to the Hong Kong server; PHP lint and the product JavaScript syntax check passed.
- `includes/footer.php` and `admin/footer.php` are synced to the Hong Kong server and passed production PHP lint.
- `admin/inquiry_routing.php`, `admin/settings.php`, `includes/default_content.php`, `includes/floating_actions.php` and `includes/inquiry_routing.php` are synced to the Hong Kong server and passed production PHP lint.
- Initial code sync to GitHub completed.
- `WORKFLOW_RULES.md` synced to server.
- `CODEX_CONTEXT.md` synced to server.

## Current Session Closeout

- SEO work follows the user-approved sequence: complete one item, validate front and back end, report, then wait for confirmation before starting the next. Sitemap/robots, URL/301/www-SSL, SEO Title/Description, NO IMAGE / duplicate product / empty-content cleanup, unified contact information, structured data, Google Search Console submission/verification, keyword allocation/internal linking and mobile speed/Core Web Vitals infrastructure are complete. The user-approved SEO foundation sequence is complete unless they request deeper performance work or another SEO pass.
- GitHub is synced on `main`. Normal GitHub SSH works without the temporary `ProxyCommand=none` override.
- Blog categories are now managed separately from blog articles at `后台 → 资源中心 → Blog & Insights → 管理博客分类`. Categories can be added, renamed, ordered, hidden and deleted. Category deletion requires choosing a destination category and moves its articles there, so no blog posts are lost.
- The supplied red-and-black A mark is now the default browser tab icon. Future changes are self-service through the Website Settings page; upload a square PNG (recommended 512 × 512) and save.
- Normal `git push origin main` may still fail while the local GitHub proxy is off; the current safe workaround is to bypass that proxy for GitHub commands only.
- Existing public application cards are now backfilled under their corresponding Solution Applications category, so the backend is populated rather than blank and remains the source of the front-end application-card list.
- The five non-Retail solution categories now have their own populated application-management lists inside `Solution Applications`; new child pages can be added in the selected category whenever needed.
- Retail Applications can now be increased or decreased from the left-side list. Creating an item opens its fully editable template; deleting an item removes it from the public application navigation while retaining uploaded media in the media library.
- The public Projects page top banner can now be changed independently in the admin without affecting the project-list images. Its image, ALT, large title and small descriptive copy are all editable in one form.
- The latest one-by-one serious-issues workflow status: Serious issue #1 (project URL/content mismatch) is fixed/confirmed. Serious issue #2 (Project template not switching by category) is fixed/confirmed. Serious issue #3 (homepage backend status text) is fixed/confirmed. Serious issue #4 (Downloads page logical fault) is fixed but the user said to ignore/skip the next empty-category issue. Serious issue #5 (empty product categories in main navigation) is skipped for now. Serious issue #6 (company years inconsistency) is fixed/confirmed. Serious issue #7 (blog template/content pollution) is fixed/confirmed. Serious issue #8 (product page highly templated content) now has a first cleanup pass plus deeper expert-content rewrites for the main Track, Downlight, Low Voltage/Magnetic, Surface/Pendant, Cabinet and selected Outdoor families, including WASHO Track, VOLI IP65 and SOFT IP65. Current DB residual scan for tracked product-family application template phrases is `remaining=0`. If the user continues, next likely priority is a lighter qualitative pass on already-specific families such as PROFOCUS, WINDOW LIGHT, ARCWASH, ORBI and Track Systems / Accessories, or moving to another SEO/content issue.
- Mobile issue #13 (FAQ text / numbers / symbols / formatting hard errors) is fixed directly in the production `web_resource_faqs` database. A backup table was created first: `web_resource_faqs_bak_20260802_155727`. Two cleanup passes updated 40 FAQ rows total, covering missing spaces, incorrect angle notation (`10°–24°`, `24°–36°`, `36°–60°`), `CRI`/`TRIAC`/`DALI` casing, `I` misread as `l`, removed `Answer` from the luminous-efficacy question, and corrected obvious glued words. `resources-faq.php` was also updated so the hidden FAQ search text preserves original casing/units such as `lm/W` instead of writing lowercased text into the HTML. Production lint passed, the final case-sensitive DB scan returned `remaining=0`, and the cache-busted public FAQ page returned 0 matches for the known bad terms.
- Mobile/product-card link rule updated: card CTAs labelled `View Series` now link to the current item's owning series page, not a concrete product detail URL. `products.php` product-result cards and their ItemList schema now use series URLs; homepage product recommendations from `artdon_product_unify_v713` now generate pretty `/products/{category}/{series}` URLs for both series and variant-backed slots. `index.php`, `products.php`, `includes/artdon_product_unify_v713.php` and `includes/home_product_publish.php` are deployed to Hong Kong; production PHP lint passed and live homepage cards now output `/products/.../...` links with no `product.php?slug=` or `series.php?slug=` card links.
- FAQ page category list now includes `About Artdon` after `Warranty & Support`; the same category is available in the admin FAQ category dropdown because both frontend and backend read `artdon_resource_faq_categories()`. Five About Artdon FAQ items were inserted/backfilled into production (`db_count=5`). `includes/resources_faq_data.php` is deployed to Hong Kong, production lint passed, and the cache-busted public FAQ page shows the `About Artdon` filter plus the new company FAQ items.
- Visitor Radar now supports a recoverable “不再记录” exclusion workflow. `web_visit_exclusions` is created by the analytics migration; `analytics_track.php` stops writing future tracking rows for excluded visitor/IP/IP-group combinations and returns `ignored=excluded`; admin visitor cards and visitor detail drawer include `不再记录` / `恢复记录`; normal Visitor Radar, KPI, recent-flow and ranking queries hide excluded profiles by default, while the top `排除名单` link shows them for review. `includes/visitor_analytics.php`, `analytics_track.php`, `admin/visitor_analytics.php` and `submit_inquiry.php` are deployed to Hong Kong; production PHP lint passed and the new table exists.
- The customer-inquiry IP blacklist panel in `admin/inquiries.php` now has its own pagination (`blacklist_page`) with 20 active IPs per page. The badge still shows the total active blacklist count, inquiry-row blocked-IP detection still checks all active blacklist IPs, and the normal inquiry list pagination remains independent. `admin/inquiries.php` is deployed to Hong Kong and production PHP lint passed.
- The customer-inquiry list now hides blacklisted-IP inquiries by default via a new `blacklist` category filter. Quick tabs and the full filter form support `正常询盘`, `已拉黑询盘`, and `全部询盘`; default is `正常询盘`, so blocked advertising submissions no longer stay mixed into the normal work queue. `admin/inquiries.php` is deployed to Hong Kong and production PHP lint passed.
- No open code changes are pending after committing the mobile hero copy record.
- Next session should continue using the default workflow:

```text
Local edit -> Git commit -> GitHub push -> Server deploy
```

## Standing Rule

Before ending a work session or when the user asks to stop/exit, update this file with the current context, then commit, push to GitHub, and sync to the server.
